const menuButton=document.querySelector('.menu-button');const mobileNav=document.getElementById('mobile-nav');if(menuButton&&mobileNav){menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));mobileNav.hidden=open;});}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const providerGrid=document.querySelector('.provider-grid');if(providerGrid){const link=document.createElement('link');link.rel='stylesheet';link.href='/assets/auth-icons.css';document.head.appendChild(link);const names=['google','facebook','x','discord','github','email'];providerGrid.querySelectorAll(':scope > span').forEach((item,index)=>{const name=names[index];if(!name)return;item.classList.add('provider-item',`provider-${name}`);item.setAttribute('aria-disabled','true');item.setAttribute('title','Inicio de sesión todavía no activo');if(name==='x'){item.textContent='';item.setAttribute('aria-label','X');}});providerGrid.addEventListener('click',()=>{const note=document.querySelector('.auth-note');if(note)note.textContent='El inicio de sesión aún no está conectado. Falta activar el backend y OAuth; no se simulará un acceso falso.';});}